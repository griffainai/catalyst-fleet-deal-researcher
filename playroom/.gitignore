# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# env
.env.local
.env*.local

# vercel
.vercel

# NOTE: .researcher/ is intentionally committed. It's the build-time mirror of the
# researcher folder so the deployed function is self-contained. scripts/sync-researcher.mjs
# refreshes it from ../ whenever the source folder is present (local dev / full-repo build).

# typescript
*.tsbuildinfo
next-env.d.ts
